// Code generated by http://github.com/gojuno/minimock (v3.4.4). DO NOT EDIT.

package mocks

//go:generate minimock -i github.com/Sane4eck55/CART-LOMS-COMMENTS-NOTIFIER/loms/internal/service.Tracer -o tracer_mock.go -n TracerMock -p mocks

import (
	"context"
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock/v3"
	"go.opentelemetry.io/otel/trace"
)

// TracerMock implements mm_service.Tracer
type TracerMock struct {
	t          minimock.Tester
	finishOnce sync.Once

	funcStart          func(ctx context.Context, spanName string, opts ...trace.SpanStartOption) (c2 context.Context, s1 trace.Span)
	funcStartOrigin    string
	inspectFuncStart   func(ctx context.Context, spanName string, opts ...trace.SpanStartOption)
	afterStartCounter  uint64
	beforeStartCounter uint64
	StartMock          mTracerMockStart
}

// NewTracerMock returns a mock for mm_service.Tracer
func NewTracerMock(t minimock.Tester) *TracerMock {
	m := &TracerMock{t: t}

	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.StartMock = mTracerMockStart{mock: m}
	m.StartMock.callArgs = []*TracerMockStartParams{}

	t.Cleanup(m.MinimockFinish)

	return m
}

type mTracerMockStart struct {
	optional           bool
	mock               *TracerMock
	defaultExpectation *TracerMockStartExpectation
	expectations       []*TracerMockStartExpectation

	callArgs []*TracerMockStartParams
	mutex    sync.RWMutex

	expectedInvocations       uint64
	expectedInvocationsOrigin string
}

// TracerMockStartExpectation specifies expectation struct of the Tracer.Start
type TracerMockStartExpectation struct {
	mock               *TracerMock
	params             *TracerMockStartParams
	paramPtrs          *TracerMockStartParamPtrs
	expectationOrigins TracerMockStartExpectationOrigins
	results            *TracerMockStartResults
	returnOrigin       string
	Counter            uint64
}

// TracerMockStartParams contains parameters of the Tracer.Start
type TracerMockStartParams struct {
	ctx      context.Context
	spanName string
	opts     []trace.SpanStartOption
}

// TracerMockStartParamPtrs contains pointers to parameters of the Tracer.Start
type TracerMockStartParamPtrs struct {
	ctx      *context.Context
	spanName *string
	opts     *[]trace.SpanStartOption
}

// TracerMockStartResults contains results of the Tracer.Start
type TracerMockStartResults struct {
	c2 context.Context
	s1 trace.Span
}

// TracerMockStartOrigins contains origins of expectations of the Tracer.Start
type TracerMockStartExpectationOrigins struct {
	origin         string
	originCtx      string
	originSpanName string
	originOpts     string
}

// Marks this method to be optional. The default behavior of any method with Return() is '1 or more', meaning
// the test will fail minimock's automatic final call check if the mocked method was not called at least once.
// Optional() makes method check to work in '0 or more' mode.
// It is NOT RECOMMENDED to use this option unless you really need it, as default behaviour helps to
// catch the problems when the expected method call is totally skipped during test run.
func (mmStart *mTracerMockStart) Optional() *mTracerMockStart {
	mmStart.optional = true
	return mmStart
}

// Expect sets up expected params for Tracer.Start
func (mmStart *mTracerMockStart) Expect(ctx context.Context, spanName string, opts ...trace.SpanStartOption) *mTracerMockStart {
	if mmStart.mock.funcStart != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Set")
	}

	if mmStart.defaultExpectation == nil {
		mmStart.defaultExpectation = &TracerMockStartExpectation{}
	}

	if mmStart.defaultExpectation.paramPtrs != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by ExpectParams functions")
	}

	mmStart.defaultExpectation.params = &TracerMockStartParams{ctx, spanName, opts}
	mmStart.defaultExpectation.expectationOrigins.origin = minimock.CallerInfo(1)
	for _, e := range mmStart.expectations {
		if minimock.Equal(e.params, mmStart.defaultExpectation.params) {
			mmStart.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmStart.defaultExpectation.params)
		}
	}

	return mmStart
}

// ExpectCtxParam1 sets up expected param ctx for Tracer.Start
func (mmStart *mTracerMockStart) ExpectCtxParam1(ctx context.Context) *mTracerMockStart {
	if mmStart.mock.funcStart != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Set")
	}

	if mmStart.defaultExpectation == nil {
		mmStart.defaultExpectation = &TracerMockStartExpectation{}
	}

	if mmStart.defaultExpectation.params != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Expect")
	}

	if mmStart.defaultExpectation.paramPtrs == nil {
		mmStart.defaultExpectation.paramPtrs = &TracerMockStartParamPtrs{}
	}
	mmStart.defaultExpectation.paramPtrs.ctx = &ctx
	mmStart.defaultExpectation.expectationOrigins.originCtx = minimock.CallerInfo(1)

	return mmStart
}

// ExpectSpanNameParam2 sets up expected param spanName for Tracer.Start
func (mmStart *mTracerMockStart) ExpectSpanNameParam2(spanName string) *mTracerMockStart {
	if mmStart.mock.funcStart != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Set")
	}

	if mmStart.defaultExpectation == nil {
		mmStart.defaultExpectation = &TracerMockStartExpectation{}
	}

	if mmStart.defaultExpectation.params != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Expect")
	}

	if mmStart.defaultExpectation.paramPtrs == nil {
		mmStart.defaultExpectation.paramPtrs = &TracerMockStartParamPtrs{}
	}
	mmStart.defaultExpectation.paramPtrs.spanName = &spanName
	mmStart.defaultExpectation.expectationOrigins.originSpanName = minimock.CallerInfo(1)

	return mmStart
}

// ExpectOptsParam3 sets up expected param opts for Tracer.Start
func (mmStart *mTracerMockStart) ExpectOptsParam3(opts ...trace.SpanStartOption) *mTracerMockStart {
	if mmStart.mock.funcStart != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Set")
	}

	if mmStart.defaultExpectation == nil {
		mmStart.defaultExpectation = &TracerMockStartExpectation{}
	}

	if mmStart.defaultExpectation.params != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Expect")
	}

	if mmStart.defaultExpectation.paramPtrs == nil {
		mmStart.defaultExpectation.paramPtrs = &TracerMockStartParamPtrs{}
	}
	mmStart.defaultExpectation.paramPtrs.opts = &opts
	mmStart.defaultExpectation.expectationOrigins.originOpts = minimock.CallerInfo(1)

	return mmStart
}

// Inspect accepts an inspector function that has same arguments as the Tracer.Start
func (mmStart *mTracerMockStart) Inspect(f func(ctx context.Context, spanName string, opts ...trace.SpanStartOption)) *mTracerMockStart {
	if mmStart.mock.inspectFuncStart != nil {
		mmStart.mock.t.Fatalf("Inspect function is already set for TracerMock.Start")
	}

	mmStart.mock.inspectFuncStart = f

	return mmStart
}

// Return sets up results that will be returned by Tracer.Start
func (mmStart *mTracerMockStart) Return(c2 context.Context, s1 trace.Span) *TracerMock {
	if mmStart.mock.funcStart != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Set")
	}

	if mmStart.defaultExpectation == nil {
		mmStart.defaultExpectation = &TracerMockStartExpectation{mock: mmStart.mock}
	}
	mmStart.defaultExpectation.results = &TracerMockStartResults{c2, s1}
	mmStart.defaultExpectation.returnOrigin = minimock.CallerInfo(1)
	return mmStart.mock
}

// Set uses given function f to mock the Tracer.Start method
func (mmStart *mTracerMockStart) Set(f func(ctx context.Context, spanName string, opts ...trace.SpanStartOption) (c2 context.Context, s1 trace.Span)) *TracerMock {
	if mmStart.defaultExpectation != nil {
		mmStart.mock.t.Fatalf("Default expectation is already set for the Tracer.Start method")
	}

	if len(mmStart.expectations) > 0 {
		mmStart.mock.t.Fatalf("Some expectations are already set for the Tracer.Start method")
	}

	mmStart.mock.funcStart = f
	mmStart.mock.funcStartOrigin = minimock.CallerInfo(1)
	return mmStart.mock
}

// When sets expectation for the Tracer.Start which will trigger the result defined by the following
// Then helper
func (mmStart *mTracerMockStart) When(ctx context.Context, spanName string, opts ...trace.SpanStartOption) *TracerMockStartExpectation {
	if mmStart.mock.funcStart != nil {
		mmStart.mock.t.Fatalf("TracerMock.Start mock is already set by Set")
	}

	expectation := &TracerMockStartExpectation{
		mock:               mmStart.mock,
		params:             &TracerMockStartParams{ctx, spanName, opts},
		expectationOrigins: TracerMockStartExpectationOrigins{origin: minimock.CallerInfo(1)},
	}
	mmStart.expectations = append(mmStart.expectations, expectation)
	return expectation
}

// Then sets up Tracer.Start return parameters for the expectation previously defined by the When method
func (e *TracerMockStartExpectation) Then(c2 context.Context, s1 trace.Span) *TracerMock {
	e.results = &TracerMockStartResults{c2, s1}
	return e.mock
}

// Times sets number of times Tracer.Start should be invoked
func (mmStart *mTracerMockStart) Times(n uint64) *mTracerMockStart {
	if n == 0 {
		mmStart.mock.t.Fatalf("Times of TracerMock.Start mock can not be zero")
	}
	mm_atomic.StoreUint64(&mmStart.expectedInvocations, n)
	mmStart.expectedInvocationsOrigin = minimock.CallerInfo(1)
	return mmStart
}

func (mmStart *mTracerMockStart) invocationsDone() bool {
	if len(mmStart.expectations) == 0 && mmStart.defaultExpectation == nil && mmStart.mock.funcStart == nil {
		return true
	}

	totalInvocations := mm_atomic.LoadUint64(&mmStart.mock.afterStartCounter)
	expectedInvocations := mm_atomic.LoadUint64(&mmStart.expectedInvocations)

	return totalInvocations > 0 && (expectedInvocations == 0 || expectedInvocations == totalInvocations)
}

// Start implements mm_service.Tracer
func (mmStart *TracerMock) Start(ctx context.Context, spanName string, opts ...trace.SpanStartOption) (c2 context.Context, s1 trace.Span) {
	mm_atomic.AddUint64(&mmStart.beforeStartCounter, 1)
	defer mm_atomic.AddUint64(&mmStart.afterStartCounter, 1)

	mmStart.t.Helper()

	if mmStart.inspectFuncStart != nil {
		mmStart.inspectFuncStart(ctx, spanName, opts...)
	}

	mm_params := TracerMockStartParams{ctx, spanName, opts}

	// Record call args
	mmStart.StartMock.mutex.Lock()
	mmStart.StartMock.callArgs = append(mmStart.StartMock.callArgs, &mm_params)
	mmStart.StartMock.mutex.Unlock()

	for _, e := range mmStart.StartMock.expectations {
		if minimock.Equal(*e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.c2, e.results.s1
		}
	}

	if mmStart.StartMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmStart.StartMock.defaultExpectation.Counter, 1)
		mm_want := mmStart.StartMock.defaultExpectation.params
		mm_want_ptrs := mmStart.StartMock.defaultExpectation.paramPtrs

		mm_got := TracerMockStartParams{ctx, spanName, opts}

		if mm_want_ptrs != nil {

			if mm_want_ptrs.ctx != nil && !minimock.Equal(*mm_want_ptrs.ctx, mm_got.ctx) {
				mmStart.t.Errorf("TracerMock.Start got unexpected parameter ctx, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmStart.StartMock.defaultExpectation.expectationOrigins.originCtx, *mm_want_ptrs.ctx, mm_got.ctx, minimock.Diff(*mm_want_ptrs.ctx, mm_got.ctx))
			}

			if mm_want_ptrs.spanName != nil && !minimock.Equal(*mm_want_ptrs.spanName, mm_got.spanName) {
				mmStart.t.Errorf("TracerMock.Start got unexpected parameter spanName, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmStart.StartMock.defaultExpectation.expectationOrigins.originSpanName, *mm_want_ptrs.spanName, mm_got.spanName, minimock.Diff(*mm_want_ptrs.spanName, mm_got.spanName))
			}

			if mm_want_ptrs.opts != nil && !minimock.Equal(*mm_want_ptrs.opts, mm_got.opts) {
				mmStart.t.Errorf("TracerMock.Start got unexpected parameter opts, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmStart.StartMock.defaultExpectation.expectationOrigins.originOpts, *mm_want_ptrs.opts, mm_got.opts, minimock.Diff(*mm_want_ptrs.opts, mm_got.opts))
			}

		} else if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmStart.t.Errorf("TracerMock.Start got unexpected parameters, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
				mmStart.StartMock.defaultExpectation.expectationOrigins.origin, *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		mm_results := mmStart.StartMock.defaultExpectation.results
		if mm_results == nil {
			mmStart.t.Fatal("No results are set for the TracerMock.Start")
		}
		return (*mm_results).c2, (*mm_results).s1
	}
	if mmStart.funcStart != nil {
		return mmStart.funcStart(ctx, spanName, opts...)
	}
	mmStart.t.Fatalf("Unexpected call to TracerMock.Start. %v %v %v", ctx, spanName, opts)
	return
}

// StartAfterCounter returns a count of finished TracerMock.Start invocations
func (mmStart *TracerMock) StartAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmStart.afterStartCounter)
}

// StartBeforeCounter returns a count of TracerMock.Start invocations
func (mmStart *TracerMock) StartBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmStart.beforeStartCounter)
}

// Calls returns a list of arguments used in each call to TracerMock.Start.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmStart *mTracerMockStart) Calls() []*TracerMockStartParams {
	mmStart.mutex.RLock()

	argCopy := make([]*TracerMockStartParams, len(mmStart.callArgs))
	copy(argCopy, mmStart.callArgs)

	mmStart.mutex.RUnlock()

	return argCopy
}

// MinimockStartDone returns true if the count of the Start invocations corresponds
// the number of defined expectations
func (m *TracerMock) MinimockStartDone() bool {
	if m.StartMock.optional {
		// Optional methods provide '0 or more' call count restriction.
		return true
	}

	for _, e := range m.StartMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	return m.StartMock.invocationsDone()
}

// MinimockStartInspect logs each unmet expectation
func (m *TracerMock) MinimockStartInspect() {
	for _, e := range m.StartMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to TracerMock.Start at\n%s with params: %#v", e.expectationOrigins.origin, *e.params)
		}
	}

	afterStartCounter := mm_atomic.LoadUint64(&m.afterStartCounter)
	// if default expectation was set then invocations count should be greater than zero
	if m.StartMock.defaultExpectation != nil && afterStartCounter < 1 {
		if m.StartMock.defaultExpectation.params == nil {
			m.t.Errorf("Expected call to TracerMock.Start at\n%s", m.StartMock.defaultExpectation.returnOrigin)
		} else {
			m.t.Errorf("Expected call to TracerMock.Start at\n%s with params: %#v", m.StartMock.defaultExpectation.expectationOrigins.origin, *m.StartMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcStart != nil && afterStartCounter < 1 {
		m.t.Errorf("Expected call to TracerMock.Start at\n%s", m.funcStartOrigin)
	}

	if !m.StartMock.invocationsDone() && afterStartCounter > 0 {
		m.t.Errorf("Expected %d calls to TracerMock.Start at\n%s but found %d calls",
			mm_atomic.LoadUint64(&m.StartMock.expectedInvocations), m.StartMock.expectedInvocationsOrigin, afterStartCounter)
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *TracerMock) MinimockFinish() {
	m.finishOnce.Do(func() {
		if !m.minimockDone() {
			m.MinimockStartInspect()
		}
	})
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *TracerMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *TracerMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockStartDone()
}
